{
  "name": "notify",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc index.ts",
    "deploy": "aws lambda update-function-code --function-name NotifyFilerr --zip-file fileb://function.zip"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "cross-fetch": "^3.1.5",
    "node-appwrite": "^8.2.0"
  }
}
